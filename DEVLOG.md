# ぽちっとQR工房 — 制作ログ

## 概要

QRコード即時生成サービスのMOCK v1。
参考デザイン（ぽちっとQR工房 UI）を基にレスポンシブ実装。
まずはQRコード生成1機能のみでデプロイし、view数を計測する方針。

---

## フェーズ 1：既存コードの確認（2026-05-10）

### 既存構成

```
index.html   — シンプルな単一カラムUI
styles.css   — モノクロ系デザイン
script.js    — qrcodejs使用のQR生成ロジック（動作済み）
README.md
```

既存の `script.js` のQR生成ロジック（qrcodejs / download）は動作していたため流用・拡張する方針にした。

---

## フェーズ 2：デザイン設計

### 参考UIから読み取った仕様

| 要素           | 仕様                                                         |
| -------------- | ------------------------------------------------------------ |
| カラー         | ディープレッド `#B91C1C` 系                                  |
| レイアウト     | 3カラム（左サイドバー／メイン／右サイドバー）                |
| フォント       | Noto Sans JP                                                 |
| 入力タイプ     | リンク・文字入力・連絡先・動画・ファイル（MOCKはリンクのみ） |
| QRカスタマイズ | メインカラー選択（5色＋カスタム）                            |

### 指摘されたデザイン問題と対応

| 問題                         | 対応                                                                                                                          |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| カラム間の赤いサイドボーダー | カラム区切りを赤から除去。カード・サイドバーの `border` は `#E5E7EB`（ライトグレー）に統一                                    |
| 右カラムが画面からはみ出る   | `max-width: 1280px` + `grid-template-columns` で3カラムを比率管理。右サイドバー幅を `220px` に固定し `padding` でバランス調整 |

---

## フェーズ 3：実装

### HTML (`index.html`)

- ヘッダー：ロゴ＋PCナビ＋ログインボタン
- モバイルナビ：ハンバーガー→ドロワー方式
- 3カラムグリッド（CSS Grid）
- 左サイドバー：カテゴリ別ナビ（静的リンク、MOCK）
- メイン：
  - Step 1：入力タイプタブ（リンクのみ有効）＋URL入力＋「生成する」ボタン
  - Step 2：カラー選択＋QRプレビュー＋ダウンロードボタン
- 右サイドバー：QR管理パネル / アカウント情報 / プレミアムプロモ / おすすめリンク / チャット

### CSS (`styles.css`)

- CSS変数で全カラーを一元管理
- ブレークポイント
  - `≤1024px` — サイドバー幅縮小
  - `≤860px` — 左サイドバー非表示、右サイドバー縦1カラム全幅、ハンバーガー表示
  - `≤520px` — QRフレーム縮小
- カラム間の区切りは全て `border: 1px solid #E5E7EB`（赤なし）

### JS (`script.js`)

- 「生成する」ボタンクリック or Enter キーでQR生成
- `qrcodejs` でcanvasにレンダリング
- カラー変更時に既存QRを再レンダリング
- PNGダウンロード（canvas.toDataURL）
- ハンバーガーメニューの開閉制御
- `href="#"` 全リンクに `preventDefault()` を付与（TOPへ戻る挙動を防止）

---

## フェーズ 4：バグ修正

| バグ                                         | 原因                                                              | 対応                                                                        |
| -------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------- |
| QRとプレースホルダーが重複表示               | `display: flex` が `hidden` 属性の `display: none` を上書き       | リセットに `[hidden] { display: none !important; }` を追加                  |
| QRがフレーム内でずれる                       | `qr-frame` が flex で `#qrcode` と `.qr-empty` を横並びにしていた | `position: relative` + `.qr-empty` を `position: absolute; inset: 0` に変更 |
| レスポンシブ時に右サイドバーが全幅にならない | `repeat(auto-fill, minmax(160px, 1fr))` で複数カラムになっていた  | `display: flex; flex-direction: column; width: 100%` に変更                 |
| モックリンクでTOPに戻る                      | `href="#"` のデフォルト動作                                       | JS で `preventDefault()` を全 `href="#"` に適用                             |

---

## フェーズ 5：デザイン調整

| 変更                 | 内容                                                                                    |
| -------------------- | --------------------------------------------------------------------------------------- |
| プロモパネルのグラデ | `linear-gradient(135deg, red, #7F1D1D)` → `var(--red)` 単色に変更（黒っぽい部分を除去） |
| アカウント情報       | 「25/50」「30日後」などのフェイクデータ → 「—」「ログインして確認」に変更               |
| ダウンロードボタン   | 「プレビューを表示 / PNGダウンロード」→「PNGダウンロード」に変更                        |

---

## フェーズ 6：セキュリティ・ドキュメント整備

### 作成ファイル（.gitignoreで除外済み・ローカルのみ）

| ファイル      | 内容                                                                |
| ------------- | ------------------------------------------------------------------- |
| `SECURITY.md` | 現状の安全な点・中リスク（SRI未設定・CSP未設定）・将来の対策一覧    |
| `DEPLOY.md`   | GitHub Pages / Vercel / Netlify の手順・CSPヘッダー設定・GA計測方法 |
| `DEVLOG.md`   | 本ファイル。制作過程の記録                                          |

### セキュリティ判断

現状（静的サイト・バックエンドなし）では中リスク項目（SRI・CSP未設定）があるが、小規模運営においてデプロイ上の問題はなし。バックエンド追加時に対処する方針。

---

## フェーズ 7：Git / GitHub 整備（2026-05-10）

### 問題発覚と対応

`C:/Users/a/` ホームディレクトリ全体が誤って git 管理されていた。

- `.ssh/`・`NTUSER.DAT`・`AppData/` 等が追跡対象になっていた
- コミット・リモート設定ゼロだったため **GitHub への流出はなし**
- `C:/Users/a/.git` を削除してホームの git 管理を解除

### プロジェクト専用リポジトリの作成

```
プロジェクトルート: c:\Users\a\Documents\security\QRcode_Gen_Website\
GitHub: https://github.com/sleepycat12341013/QRcode_Gen_Website
ブランチ: main
```

`.gitignore` で `*.md` を除外し、pushしたファイルは以下4つのみ：

```
index.html / styles.css / script.js / .gitignore
```

---

## フェーズ 8：Google Analytics 設置（2026-05-10）

- GA4 プロパティ新規作成（既存プロパティとの共存は問題なし）
- プラットフォーム：**ウェブ** を選択
- 測定ID：`G-1G6VC4NMYL`
- `index.html` の `<head>` に gtag スニペットを埋め込み
- GitHub push → Vercel 自動デプロイで反映

---

## 現在の状態（2026-05-10時点）

### 実装済み

- [x] URL入力 → QRコード即時生成
- [x] QRカラー変更（5プリセット＋カスタムカラーピッカー）
- [x] PNGダウンロード
- [x] レスポンシブ対応（PC / タブレット / スマホ）
- [x] モバイルハンバーガーナビ
- [x] Google Analytics（G-1G6VC4NMYL）
- [x] GitHub（private）+ Vercel デプロイ済み

### 次のアクション

- GA 反映待ち 5/12 8:00 予定
- [ ] Google AdSense 申請
  - [ ] プライバシーポリシーページ（`privacy.html`）の作成 ← **申請前に必須**
  - [ ] お問い合わせページの作成（審査通過率向上）
  - [ ] フッターにプライバシーポリシーへのリンクを追加
  - [ ] AdSense コード埋め込み・審査提出

### 将来の機能追加候補（優先度順）

1. テキスト・連絡先QR（バックエンド不要）
2. QRデザインパターン選択
3. ロゴ合成
4. ユーザー認証・マイQR保存（Supabase等）
5. 短縮URL・アクセス解析
6. API連携
